[server]
# Exclude torch internals from the file-watcher (stops the torch.classes traceback spam)
folderWatchBlacklist = ["torch", "torch.*"]
# Allow embedding in iframe from the React frontend
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false

[theme]
base = "dark"
